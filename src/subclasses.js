import Character from "./mainclass";

export class Magician extends Character {
  constructor(baseAttack, distance) {
    super(baseAttack, distance);
  }
}

export class Daemon extends Character {
  constructor(baseAttack, distance) {
    super(baseAttack, distance);
  }
}
